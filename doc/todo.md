# KeyRecord 未完成事项

当前可视化服务已迁移为 C++ keyrecord_server，旧 Node.js 可视化服务已归档到
archive/node-server/visualize/。本文仅保留当前代码库中尚未完成、且仍适用于当前架构的事项。

## API 与数据能力

- [x] 增加 GET /api/hourly-stats?date=YYYY-MM-DD，返回 [{hour, count}]，用于小时级热力分布。
- [x] 为 API 日期参数增加格式校验和非法范围处理。
- [x] 为 /api/keys 的 limit 增加上限，避免异常大请求触发重查询。

## 前端可视化

- [x] 将日期范围筛选升级为 D3 brush 滑块。
- [x] 补齐快捷范围按钮：今天、最近 30 天、本月、全部。
- [x] 键盘热力图增加缩放和平移能力（可选）。
- [x] 增加深色/浅色主题切换（可选）。
- [x] 增加加载动画或加载进度条。
- [x] 增加数据导出：CSV、JSON、PNG。
- [x] 增加打印优化。

## 高级分析功能

- [x] 小时级热力图：`/api/hourly-heatmap` 返回 [{weekday, hour, count}]，前端渲染 7×24 网格。
- [x] 按键时序动画回放：`/api/timeline` 返回按 id 顺序的事件 [{ts, vk_code, key_name}]，前端支持变速播放、进度条、键盘热力图联动高亮。
- [x] 多键组合统计：`/api/combos` 扫描有序事件统计 Ctrl+C 等修饰键组合，返回 [{combo, count}]。
- [x] 键盘分区统计：`/api/region-stats` 返回 [{region, count}]，分区包括字母区、数字区、小键盘、功能键、导航键、修饰键、控制键、标点符号、其他。
- [x] 左右手使用频率对比：`/api/hand-stats` 返回 [{hand, count}]，基于标准盲打指法归类左/右/双手/未知。
- [x] 输入速度曲线图：`/api/speed` 返回某日按分钟桶聚合的击键数 [{minute, count}]，前端 SVG 折线图渲染。
- [x] 不同日期或时间段的数据对比：前端提供范围 A/B 选择器，对比分区统计、左右手统计、总击键数等指标。
- [x] 实时监控模式：前端 WebSocket 轮询模式（每 5 秒刷新），可配置回调刷新当前范围数据。

## 性能与数据维护

- [x] API 响应缓存或查询结果缓存。
- [x] 前端数据缓存，例如 LocalStorage。
- [x] 按需加载数据，避免未来大范围统计一次性加载过多。
- [x] 增加统计汇总表，避免长期依赖 keys 明细表做全量聚合。
- [x] 定义明细数据保留、归档和压缩策略。

## 部署与安装

- [ ] 配置开机自启动。
- [ ] 提供安装脚本。
- [ ] 评估是否需要单文件可执行程序打包。

## 测试

- [ ] 前后端数据流集成测试。
- [ ] 日期范围筛选联动测试。
- [ ] 日历图点击联动测试。
- [ ] 浏览器兼容性测试：Chrome、Firefox、Edge。
- [ ] 大数据量加载性能测试。
- [ ] 热力图渲染性能测试。
- [ ] 内存泄漏检测。
